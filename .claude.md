# Claude Workspace Instructions: Aegis Governance

This workspace is governed by the **Aegis Architectural Microkernel**. Adherence to the **Agent-Native Governance Protocol** is mandatory for all Claude Code agents.

---

## ⚡ Native Integration

Your environment is pre-configured with the **Aegis MCP Server**. You must integrate it into your cognition loop using these core protocols:

### 1. The Validation Gate
You **MUST NOT** declare a task complete until you have called:
> `validate_architecture_compliance(files_modified=[...])`

- Apply structural remediations immediately.
- Re-run validation until **SUCCESS** is returned.

### 2. Semantic Self-Evaluation
Aegis enforces design intent through **Semantic Rubrics**. If a rubric is returned:
- Apply your reasoning to judge code compliance.
- Output violations in the specified `VIOLATION: ... - FIX: ...` format.
- Fix and re-verify.

---

## 🤝 Multi-Agent Coordination

Check the **Coordination Info** in validation reports for handoff context from other agents (Aider, Gemini). 
- Use the `handoff_note` parameter in `validate_architecture_compliance` to share your own technical decisions with future agents.

---

## 🛠️ Key Resources

- **`AGENTS.md`**: Detailed governance protocols.
- **`.aegis/rules/`**: The project's architectural laws.
- **`/aegis-principal-architect`**: Your default persona. Load it to steer the project.

**Invariant**: All code generation must align with the laws in `.aegis/rules/`.
