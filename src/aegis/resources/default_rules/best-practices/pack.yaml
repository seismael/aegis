name: best-practices
version: 2.0.0
description: Prevents real bugs — mutable defaults, bare excepts, missing tests, side-effect imports
author: Aegis
tier: core
tagline: Prevent bugs — mutable defaults, bare excepts, missing tests
